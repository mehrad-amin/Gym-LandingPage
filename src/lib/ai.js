import OpenAI from "openai";

const MAX_MODEL_ATTEMPTS = 3;

const EXCLUDED_MODEL_PATTERNS = [
  "whisper",
  "guard",
  "speech",
  "tts",
  "audio",
  "embedding",
  "vision",
];

function isTextGenerationModel(model) {
  const name = model.id.toLowerCase();

  return (
    model.active === true &&
    !EXCLUDED_MODEL_PATTERNS.some((pattern) => name.includes(pattern))
  );
}

function getModelScore(model) {
  const name = model.id.toLowerCase();

  let score = 0;

  // اولویت مدل‌های مناسب برای تولید متن
  if (name.includes("instruct")) score += 30;
  if (name.includes("versatile")) score += 25;

  // مدل‌های بزرگ‌تر معمولاً کیفیت استدلال/تولید بالاتری دارند
  if (name.includes("120b")) score += 120;
  else if (name.includes("70b")) score += 100;
  else if (name.includes("32b")) score += 80;
  else if (name.includes("27b")) score += 70;
  else if (name.includes("20b")) score += 65;
  else if (name.includes("14b")) score += 50;
  else if (name.includes("8b")) score += 30;
  else if (name.includes("3b")) score += 10;
  else if (name.includes("1b")) score -= 30;

  // مدل‌های reasoning
  if (name.includes("reasoning") || name.includes("reasoner")) {
    score += 25;
  }

  return score;
}

function isValidDietOutput(text) {
  if (!text || text.length < 500) {
    return false;
  }

  const requiredSections = [
    "هدف اصلی",
    "رویکرد تغذیه‌ای",
    "صبحانه",
    "میان‌وعده اول",
    "ناهار",
    "میان‌وعده عصر",
    "شام",
    "راهنمای جایگزینی سریع",
    "۳ اصل کلیدی",
  ];

  return requiredSections.every((section) => text.includes(section));
}

function cleanAIResponse(rawText) {
  if (!rawText) return "";

  let text = rawText;

  // حذف think tags
  text = text.replace(/<think>[\s\S]*?<\/think>/gi, "");

  // حذف markdown code fence
  text = text
    .replace(/```markdown/gi, "")
    .replace(/```/g, "")
    .trim();

  // پیدا کردن ابتدای واقعی پاسخ
  const startIndex = text.indexOf("## 🎯 اهداف و استراتژی تغذیه");

  if (startIndex !== -1) {
    text = text.substring(startIndex);
  }

  // اگر مدل با عنوان بدون ایموجی شروع کرده بود
  const fallbackStart = text.indexOf("اهداف و استراتژی تغذیه");

  if (startIndex === -1 && fallbackStart !== -1) {
    text = text.substring(fallbackStart);
  }

  return text.trim();
}

export async function fetchDietFromAI(studentDetailsText) {
  const apiKey = process.env.GROQ_API_KEY;

  if (!apiKey) {
    console.error("❌ GROQ_API_KEY is missing!");
    return "خطا: کلید API یافت نشد.";
  }

  const openai = new OpenAI({
    baseURL: "https://api.groq.com/openai/v1",
    apiKey,
    timeout: 35000,
  });

  const prompt = `
تو یک دستیار هوش مصنوعی تخصصی برای مربی فیتنس هستی.

وظیفه تو تولید یک «پیش‌نویس برنامه غذایی شخصی‌سازی‌شده» بر اساس اطلاعات دقیق ورزشکار است.

این خروجی باید توسط مربی بررسی و در صورت نیاز اصلاح شود و نباید خودت را به عنوان پزشک، متخصص تغذیه بالینی یا مرجع پزشکی معرفی کنی.

━━━━━━━━━━━━━━━━━━━━━━
اطلاعات ورزشکار
━━━━━━━━━━━━━━━━━━━━━━

${studentDetailsText}

━━━━━━━━━━━━━━━━━━━━━━
قوانین بسیار مهم محاسباتی
━━━━━━━━━━━━━━━━━━━━━━

1. عدد TDEE و Target Calories که در اطلاعات ورزشکار ارائه شده‌اند را معتبر در نظر بگیر.

2. هرگز TDEE یا Target Calories را دوباره حدس نزن و تغییر نده.

3. اگر هدف ورزشکار «افزایش حجم و عضله‌سازی» است، از TARGET CALORIES مربوط به حجم استفاده کن.

4. اگر هدف «کاهش وزن / کات» است، از TARGET CALORIES مربوط به کات استفاده کن.

5. اگر هدف «حفظ وزن» است، از TDEE به عنوان کالری هدف استفاده کن.

6. کالری هدف مهم‌ترین Constraint برنامه است.

7. برنامه نهایی باید از نظر کالری تا حد ممکن به Target Calories نزدیک باشد.
اختلاف قابل قبول:
حداکثر حدود ±5 درصد.

مثال:
اگر Target Calories = 2681 باشد،
برنامه باید تقریباً در محدوده 2547 تا 2815 kcal قرار بگیرد.

8. هرگز فقط در ابتدای برنامه ننویس که کالری هدف 2681 است؛
خود وعده‌های غذایی نیز باید تقریباً همین مقدار انرژی را تأمین کنند.

━━━━━━━━━━━━━━━━━━━━━━
محاسبه Macro Target
━━━━━━━━━━━━━━━━━━━━━━

قبل از طراحی وعده‌ها، یک هدف تقریبی برای Macronutrients تعیین کن.

برای فرد سالم و ورزشکار، پروتئین را متناسب با وزن بدن تنظیم کن.

برای هدف عضله‌سازی:
- پروتئین را معمولاً در محدوده مناسب برای فرد فعال قرار بده.
- چربی را در محدوده متعادل نگه دار.
- باقی کالری را عمدتاً از کربوهیدرات‌ها تأمین کن.

مقدار دقیق Macro Target باید با وزن، هدف و کالری فرد سازگار باشد.

در خروجی، مقدار تقریبی زیر را نمایش بده:

- کالری هدف
- پروتئین هدف
- کربوهیدرات هدف
- چربی هدف

نکته:
مجموع کالری حاصل از Macroها باید با کالری هدف سازگار باشد.

━━━━━━━━━━━━━━━━━━━━━━
قوانین طراحی وعده‌ها
━━━━━━━━━━━━━━━━━━━━━━

برنامه را در 5 وعده طراحی کن:

1. صبحانه
2. میان‌وعده اول
3. ناهار
4. میان‌وعده عصر
5. شام

برای وعده‌های اصلی، در صورت امکان دو گزینه ارائه کن:

گزینه الف
گزینه ب

اما این قانون بسیار مهم است:

اگر برای یک وعده گزینه A و B ارائه می‌کنی، کالری و پروتئین آنها باید تا حد امکان به یکدیگر نزدیک باشند.

کاربر نباید با انتخاب گزینه B نسبت به گزینه A مقدار بسیار متفاوتی کالری یا پروتئین دریافت کند.

━━━━━━━━━━━━━━━━━━━━━━
مقدار مواد غذایی
━━━━━━━━━━━━━━━━━━━━━━

هیچ‌وقت از مقادیر مبهم مانند:

«یک کف دست مرغ»
«کمی برنج»
«مقداری ماست»
«یک بشقاب برنج»

استفاده نکن.

تا حد امکان مقدار دقیق ارائه کن:

- گرم
- میلی‌لیتر
- عدد
- قاشق غذاخوری
- قاشق چای‌خوری

مثال:

سینه مرغ پخته: 150 گرم
برنج پخته: 250 گرم
ماست: 200 گرم
موز: 1 عدد متوسط

مقادیر باید با کالری و Macro Target سازگار باشند.

━━━━━━━━━━━━━━━━━━━━━━
قوانین اقتصادی
━━━━━━━━━━━━━━━━━━━━━━

اگر ورزشکار درخواست غذای ارزان، اقتصادی یا در دسترس کرده است:

اولویت با مواد غذایی ساده و رایج باشد.

اولویت بالا:

- تخم‌مرغ
- مرغ
- شیر
- ماست
- پنیر
- عدس
- لوبیا
- نخود
- برنج
- سیب‌زمینی
- نان
- جو دوسر
- موز
- سیب
- سبزیجات فصلی

از استفاده غیرضروری از مواد غذایی گران مانند:

- سالمون
- آجیل‌های گران
- کینوا
- مکمل‌های خاص
- مواد غذایی وارداتی

خودداری کن.

اگر ماده غذایی گران برای تنوع ارائه می‌شود، یک جایگزین اقتصادی برای آن نیز ارائه کن.

━━━━━━━━━━━━━━━━━━━━━━
قوانین هدف عضله‌سازی
━━━━━━━━━━━━━━━━━━━━━━

اگر هدف ورزشکار افزایش حجم و عضله‌سازی است:

- کالری هدف حجم را رعایت کن.
- پروتئین کافی تأمین کن.
- کربوهیدرات کافی برای حمایت از تمرین در نظر بگیر.
- چربی‌های غذایی را حذف نکن.
- وعده‌ها را بین روز توزیع کن.
- از افزایش افراطی کالری خودداری کن.

اگر ورزشکار مبتدی است، برنامه را بیش از حد پیچیده نکن.

غذاهای ساده، قابل تهیه و قابل تکرار اولویت دارند.

━━━━━━━━━━━━━━━━━━━━━━
محدودیت‌های غذایی
━━━━━━━━━━━━━━━━━━━━━━

فقط در صورتی محدودیت غذایی را اعمال کن که در اطلاعات ورزشکار ذکر شده باشد.

برای مثال:

اگر ورزشکار نگفته Gluten Free است،
به صورت پیش‌فرض رژیم بدون گلوتن ایجاد نکن.

اگر آلرژی، عدم تحمل غذایی یا محدودیت خاصی ذکر شده،
آن را در تمام وعده‌ها رعایت کن.

━━━━━━━━━━━━━━━━━━━━━━
قوانین محتوای خروجی
━━━━━━━━━━━━━━━━━━━━━━

خروجی باید کاملاً فارسی باشد.

از تولید این موارد خودداری کن:

- متن انگلیسی
- توضیح درباره فرآیند فکر کردن
- Chain of Thought
- <think>
- self-correction
- متن‌هایی مانند:
  "I should..."
  "But..."
  "Rule:"
  "I need to..."
  "Let me..."
- توضیح درباره Prompt
- توضیح درباره مدل هوش مصنوعی
- کد
- JSON خام
- Markdown code fence

فقط نتیجه نهایی برنامه غذایی را تولید کن.

━━━━━━━━━━━━━━━━━━━━━━
ساختار خروجی
━━━━━━━━━━━━━━━━━━━━━━

خروجی دقیقاً با ساختار زیر باشد:

## 🎯 اهداف و استراتژی تغذیه

هدف اصلی:
...

کالری هدف:
... kcal

پروتئین هدف:
... g

کربوهیدرات هدف:
... g

چربی هدف:
... g

توضیح کوتاه درباره استراتژی تغذیه:
...

## 🍳 ۱. صبحانه

### گزینه الف
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

### گزینه ب
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

## 🍎 ۲. میان‌وعده اول

- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

## 🍚 ۳. ناهار

### گزینه الف
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

### گزینه ب
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

## 🥛 ۴. میان‌وعده عصر

- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

## 🍗 ۵. شام

### گزینه الف
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

### گزینه ب
- ماده غذایی — مقدار
- ماده غذایی — مقدار

کالری تقریبی:
...
پروتئین تقریبی:
...

## 🔄 راهنمای جایگزینی سریع

پروتئین:
...

کربوهیدرات:
...

چربی:
...

## 📊 جمع‌بندی روزانه

کالری تقریبی:
... kcal

پروتئین:
... g

کربوهیدرات:
... g

چربی:
... g

مقایسه با Target Calories:
...

## 💡 ۳ اصل کلیدی

1. ...
2. ...
3. ...

━━━━━━━━━━━━━━━━━━━━━━
کنترل نهایی قبل از پاسخ
━━━━━━━━━━━━━━━━━━━━━━

قبل از تولید پاسخ، در سکوت این موارد را بررسی کن:

✓ آیا هدف ورزشکار درست تشخیص داده شده؟
✓ آیا Target Calories درست استفاده شده؟
✓ آیا مجموع کالری وعده‌ها تقریباً به Target Calories می‌رسد؟
✓ آیا پروتئین روزانه کافی و منطقی است؟
✓ آیا Macroها با کالری هدف سازگارند؟
✓ آیا گزینه‌های A/B اختلاف شدید کالری ندارند؟
✓ آیا مواد غذایی با درخواست اقتصادی بودن سازگارند؟
✓ آیا مقدار مواد غذایی مشخص و قابل اندازه‌گیری است؟
✓ آیا محدودیت غذایی بدون دلیل اضافه نشده؟
✓ آیا هیچ متن انگلیسی یا Chain of Thought در خروجی وجود ندارد؟
✓ آیا خروجی دقیقاً طبق ساختار مشخص‌شده است؟

فقط پس از انجام این کنترل‌ها، نتیجه نهایی را ارائه کن.
`;

  try {
    // دریافت مدل‌های فعال فعلی Groq
    const modelsResponse = await openai.models.list();

    const candidateModels = modelsResponse.data
      .filter(isTextGenerationModel)
      .sort((a, b) => getModelScore(b) - getModelScore(a))
      .slice(0, MAX_MODEL_ATTEMPTS);

    console.log(
      "🤖 Candidate models:",
      candidateModels.map((m) => m.id),
    );

    if (!candidateModels.length) {
      console.error("❌ No suitable text models found.");
      return "خطا: مدل مناسب برای تولید برنامه غذایی یافت نشد.";
    }

    for (const model of candidateModels) {
      try {
        console.log(`🤖 Trying model: ${model.id}`);

        const completion = await openai.chat.completions.create({
          model: model.id,

          messages: [
            {
              role: "system",
              content:
                "فقط پاسخ نهایی برنامه غذایی را به زبان فارسی تولید کن. هرگز فرآیند فکر کردن یا توضیحات داخلی خود را نمایش نده.",
            },
            {
              role: "user",
              content: prompt,
            },
          ],

          temperature: 0.2,
          max_tokens: 2200,

          // در مدل‌هایی که reasoning دارند،
          // اجازه نمایش reasoning را نده
          reasoning_format: "hidden",
        });

        const rawText = completion.choices?.[0]?.message?.content || "";

        const cleanText = cleanAIResponse(rawText);

        if (isValidDietOutput(cleanText)) {
          console.log(`✅ Valid diet generated by ${model.id}`);

          return cleanText;
        }

        console.warn(`⚠️ Invalid output from ${model.id}`);
      } catch (err) {
        const status = err?.status;

        console.warn(`⚠️ Model ${model.id} failed`, {
          status,
          message: err?.message,
        });

        // 429 یعنی محدودیت نرخ/توکن
        // مدل بعدی را امتحان کن
        if (status === 429) {
          console.warn(`⏳ Rate limit reached for ${model.id}`);
        }

        // 400/404/403 نیز می‌تواند نشان دهد
        // مدل برای این درخواست قابل استفاده نیست
        continue;
      }
    }
  } catch (err) {
    console.error("❌ Failed to retrieve/use Groq models:", err);
  }

  return "خطا در تدوین برنامه غذایی. لطفاً مجدداً تلاش کنید.";
}
